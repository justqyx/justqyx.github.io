<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">

  <title><![CDATA[Category: ruby | JustQyx]]></title>
  <link href="http://www.justqyx.me/blog/categories/ruby/atom.xml" rel="self"/>
  <link href="http://www.justqyx.me/"/>
  <updated>2016-08-11T09:42:02+08:00</updated>
  <id>http://www.justqyx.me/</id>
  <author>
    <name><![CDATA[邱源鑫]]></name>
    <email><![CDATA[hzuqiuyuanxin@gmail.com]]></email>
  </author>
  <generator uri="http://octopress.org/">Octopress</generator>

  
  <entry>
    <title type="html"><![CDATA[Ruby App Server 常用的三种 I/O 模型]]></title>
    <link href="http://www.justqyx.me/blog/2016/02/17/ruby-app-server/"/>
    <updated>2016-02-17T11:06:27+08:00</updated>
    <id>http://www.justqyx.me/blog/2016/02/17/ruby-app-server</id>
    <content type="html"><![CDATA[<p>所有的 Web 应用都遵循着一个基本模型。首先，它们需要从一些 I/O 通道接收 HTTP 请求，
然后处理这些请求，最后输出 HTTP 响应体，这个响应体将会被发送回 HTTP 客户端。
这个 HTTP 客户端通常是 Web 浏览器，又或者是一些像 curl 这一类的工具，又或者是搜索引擎的爬虫。</p>

<h2>Ruby app server</h2>

<p>Ruby app server 其实就是 HTTP Server，能与 HTTP client 直接进行对话的服务。<br/>
Ruby 里常见的 app server 的有 thin、Unicorn、Puma、Passenger。</p>

<h2>Ruby web app</h2>

<p>Ruby web app 是基于像 Rails、Sinatra 这一类框架所构建出来的应用，它并不直接与 HTTP 打交道。</p>

<p>如果 Ruby web app 直接与 HTTP 请求打交道的话，那么每一个 Ruby Web app 都需要实现与 HTTP 打交道，
所以为了简化这一过程，Ruby web app 只需要与一个被抽象过的 HTTP 请求和响应打交道，在 Ruby 生态里，最流行的便是 Rack。</p>

<p>Unicorn、Puma 和 Passenger 这一类的 app server 实现了 Rack 接口，因此 Ruby web app 能够和 Ruby app server
进行无缝对接。</p>

<p>{% img /downloads/images/rack.png %}</p>

<p>理解完 Ruby app server 和 Ruby web app 的概念后，让我们来看看常见的三种连接处理和 I/O 模型（Connection handling and I/O models）。</p>

<h2>Multi-process blocking I/O</h2>

<p>{% img /downloads/images/multi-process-io.png %}</p>

<p>一个进程每次只处理一个客户端（的请求），通过多进程的方式来实现并发。</p>

<p>读操作会被阻塞如果另一端没有发送任何数据，同样写操作也会被阻塞如果另一端接收数据太慢。
因为这样的特性，所以一个进程每次只能处理一个客户端（的请求）。
想要处理多少个客户端（的请求），就需要多少个进程。</p>

<p>这是一种传统的 I/O 模型，被 Unicorn 和 <a href="http://httpd.apache.org/docs/current/mod/prefork.html">Apache with the prefork NPM</a> 所采用。</p>

<p><strong>优点</strong></p>

<ul>
<li>使用非常简单</li>
<li>线程安全</li>
</ul>


<p><strong>缺点</strong></p>

<ul>
<li>进程开销大</li>
<li>I/O 阻塞导致吞吐量低</li>
</ul>


<h2>Multi-threaded blocking I/O</h2>

<p>{% img /downloads/images/multithreaded-io.png %}</p>

<p>I/O 调用仍然会阻塞，但通过创建线程来替代进程。
一个进程可以有许多线程，每一个线程一次处理一个客户端（的请求）。
因为线程是轻量级的，你可以使用更少的内存处理同样数量的的 I/O 并发。
要服务 5000 个 websocket 客户端，你总共需要 5000 个线程，假设你在一个台8核的机器上运行了
8 个应用进程，那么每个进程需要配置 625 个线程，这对于 Ruby 和 OS 来说是非常容易的。
一个进程可能需要 1GB 或者更少的内存，这样对于 8 个进程来说需要 8GB 的内存，
比起多进程阻塞 I/O 模型来说的 1.2TB 来说是非常小的。</p>

<p>这个 I/O 模型被 Torquebox 和 <a href="http://httpd.apache.org/docs/current/mod/worker.html">Apache with the worker MPM</a> 所使用。
也是 Puma 经常使用的模型之一，Puma 采用了一种带限制的混合策略。</p>

<p><strong>优点</strong></p>

<ul>
<li>对于类似 <a href="https://en.wikipedia.org/wiki/Embarrassingly_parallel">embarassingly parallel</a> 的工作负载，
比如 web 请求，使用线程来处理客户端 I/O 还是非常容易的。</li>
</ul>


<p><strong>缺点</strong></p>

<ul>
<li>应用程序所有的代码和依赖的库必须是线程安全的</li>
<li>应用服务必须被反向代理保护，原因和多进程 I/O 模型一样，相比较而言，虽然多线程的应用服务不太容易受 slow client 影响，
但是也并没有完全解决 slow client 的问题</li>
</ul>


<h2>Evented I/O</h2>

<p>{% img /downloads/images/event-io.png %}</p>

<p>在这种模型里，I/O 调用完全不会被阻塞。
当另一端没有发送数据，或者接收数据太慢，I/O 调用只是返回一个特定的错误。
应用程序有一个持续监听并响应 I/O 事件的事件循环，当没有事件时，该循环会进入睡眠状态。</p>

<p>这是目前最为怪异（weirdest）和难以编写的一种 I/O 模型。
它需要与前面两种模型完全不同的方法，当多进程阻塞 I/O 模型的代码非常容易转换成为多线程阻塞 I/O 模型的代码，
但是如果使用 Evented I/O 模型的话，通常情况下代码需要重写。使用 Evented I/O 模型的程序需要特定的设计。</p>

<p>这样的 I/O 模型被 Nignx、Nodejs 和 Thin 所采用，同时它也被 Puma 部分采用以限制 slow client 达到保护的目的。</p>

<p><strong>优点</strong></p>

<ul>
<li>使用这个模型你可以仅仅使用一个进程和一个线程便可以处理无限量的 I/O 并发，同时资源使用也非常少。
虽然多线程已经能够处理非常多的 I/O 并发，但 Evented I/O 还要多得多。</li>
<li>采用该模型的服务器，对 slow client 是免疫的（immune），所以可以不需要一个缓冲反向代理。</li>
</ul>


<p><strong>缺点</strong></p>

<ul>
<li>相比于阻塞型 I/O 模型，事件模型要难掌握地多，并且需要你时刻记住应用代码和库要适配该模型。</li>
</ul>


<h2>经验</h2>

<p>Nginx + Unicorn 是一个不错的选择，并且 Unicorn 支持 hot restart，部署过程对用户的使用影响较小。</p>

<p>如果遇到 websocket 这一类的需求，那么 Puma (support hot restart) 单独部署这需求相关的功能是不错的。</p>

<p>如果遇到高 I/O 的场景，如消息群发通知，那么使用 Node来实现与外部服务交互，性能是比较理想的。相信 Rubist 都会 Node。</p>

<h2>Ref</h2>

<ul>
<li><a href="http://www.rubyraptor.org/how-we-made-raptor-up-to-4x-faster-than-unicorn-and-up-to-2x-faster-than-puma-torquebox">How we made raptor up to 4x faster than unicorn and up to 2x faster than puma torquebox</a></li>
</ul>

]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[Building a DSL in Ruby]]></title>
    <link href="http://www.justqyx.me/blog/2016/02/16/build-a-dsl-in-ruby/"/>
    <updated>2016-02-16T10:23:27+08:00</updated>
    <id>http://www.justqyx.me/blog/2016/02/16/build-a-dsl-in-ruby</id>
    <content type="html"><![CDATA[<p>DSL（领域专用语言，Domain Specific Language）不是一个新的技术概念，并且构建是困难的。
其至少包含两个内容：</p>

<ol>
<li>一个用于解析 DSL 的 Parser</li>
<li>使用 DSL 写出来的程序</li>
</ol>


<h2>构建一个简单的 Quiz DSL</h2>

<p>最终我们想达到的效果便是，当运行程序之后，会让用户做选择题，并且统计回答正确的题数。</p>

<pre><code>Who was the first president of the USA?
1 - Fred Flintstone
2 - Martha Washington
3 - George Washington
4 - George Jetson
Enter your answer:
</code></pre>

<p>期望的表达语法</p>

<pre><code class="ruby"># questions.qm

question "Who was the first president of the USA?"
wrong 'Fred Flintstone'
wrong 'Martha Washington'
right 'George Washington'
wrong 'George Jetson'

question 'Who is buried in Grant\'s tomb?'
right 'U. S. Grant'
wrong 'Cary Grant'
wrong 'Hugh Grant'
wrong 'W. T. Grant'
</code></pre>

<p>一个简单的 Parser</p>

<pre><code># quizm.rb

def question text
  puts "Just read a question: #{text}"
end

def wrong text
  puts "Just read a correct answer: #{text}"
end

def right text
  puts "Just read an incorrect answer: #{text}"
end

load './questions.qm'
</code></pre>

<p>编译的 Parse Phase，最终都是为了得到 AST（抽象语法树）这样类型的数据结构，所以，我们
先定义我们的数据结构：<code>Question</code> 和 <code>Answer</code></p>

<pre><code class="ruby"># quiz.rb
class Answer
  attr_reader :text, :correct
  def initialize text, correct
    @text = text
    @correct = correct
  end
end

class Question
  def initialize text
    @text = text
    @answers = []
  end

  def add_answer answer
    @answers &lt;&lt; answer
  end

  def ask
    puts ""
    puts "Question: #{@text}"
    @answers.each_with_index do |answer, index|
      puts "#{index+1} - #{answer.text}"
    end
    print "Enter answer: "
    answer = gets.to_i - 1
    return @answers[answer].correct
  end
end
</code></pre>

<p>构建简单的 vm，赋予程序存储 DSL 内容的能力</p>

<pre><code class="ruby"># quiz.rb
require 'singleton'

class Quiz
  def initialize
    @questions = []
  end

  def add_question question
    @questions &lt;&lt; question
  end

  def last_question
    @questions.last
  end

  def run_quiz
    count = 0
    @questions.each { |q| count += 1 if q.ask }
    puts "You got #{count} answers correct out of #{@questions.size}"
  end
end
</code></pre>

<p>载入、解析、存储、运行</p>

<pre><code class="ruby">require './quiz'

def question text
  Quiz.instance.add_question Question.new(text)
end

def wrong text
  Quiz.instance.last_question.add_answer Answer.new(text, true)
end

def right text
  Quiz.instance.last_question.add_answer Answer.new(text, false)
end

load './questions.qm'

Quiz.instance.run_quiz
</code></pre>

<h2>Gif 效果图</h2>

<p>{% img /downloads/images/dsl.gif %}</p>

<h2>Ref</h2>

<ul>
<li><a href="http://jroller.com/rolsen/entry/building_a_dsl_in_ruby">Building a DSL in Ruby</a></li>
</ul>

]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[Captcha in Ruby]]></title>
    <link href="http://www.justqyx.me/blog/2015/05/16/captcha-in-ruby/"/>
    <updated>2015-05-16T11:35:41+08:00</updated>
    <id>http://www.justqyx.me/blog/2015/05/16/captcha-in-ruby</id>
    <content type="html"><![CDATA[<p>图片验证码的使用场景就不用说了，在这里分享一下我是如何做登录图片验证码的。</p>

<h2>图片验证码的生成的简单流程</h2>

<p><code>生成验证码字符</code> -> <code>生成图片验证码</code> [-> <code>使用</code>]</p>

<p>第三步的意思是，当你拿到图片验证码时，看你的具体需求了，是直接随着 form 表单渲染，
还是浏览器异步地请求图片验证码，或者其它什么的，反正无所谓了。</p>

<h2>谈一谈 simple_captcha2 的做法</h2>

<p>simple_captcha2 是基于 ImageMagick 来生成图片，基于数据库来存储验证码数据的。
提供了 <code>Controller Based</code> 和 <code>Model Based</code> 两种用法。</p>

<p><strong>Controller Based</strong></p>

<p>simple_captcha2 提供了一对方法： <code>show_simple_captcha</code> 和 <code>simple_captcha_valid?</code>。
前者是一个 view helper，让我们能够在某个地方显示一张验证码图片，后者让我们可以去判定用户提交的验证码是否正确。
想知道更多的用法可以移步到<a href="https://github.com/pludoni/simple-captcha">它的文档</a>。</p>

<p><strong>实现原理</strong></p>

<p>simple_captcha2 基于数据库维护了一对 key -> value 数据，数据结构如下：</p>

<pre><code>                                     Table "public.simple_captcha_data"
   Column   |            Type             |                            Modifiers
------------+-----------------------------+------------------------------------------------------------------
 id         | integer                     | not null default nextval('simple_captcha_data_id_seq'::regclass)
 key        | character varying(40)       |
 value      | character varying(6)        |
 created_at | timestamp without time zone |
 updated_at | timestamp without time zone |
Indexes:
    "simple_captcha_data_pkey" PRIMARY KEY, btree (id)
    "simple_captcha_data_key" btree (key)
</code></pre>

<p>当我们调用 <code>show_simple_captcha</code> 时就会生成一对这样的值，然后存放到数据库（MySQL|Postgresql|Redis）， 然后在 view 里生成这样的内容。</p>

<pre><code class="html">&lt;input type="hidden", name="captcha_key" value="d850ec25ca962ba6606cfe7c84f9568c8473e93e"
&lt;img alt="captcha" src="http://www.justqyx.me/simple_captcha?code=d850ec25ca962ba6606cfe7c84f9568c8473e93e&amp;time=1431749439" /&gt;
</code></pre>

<p>这样，它的工作原理就很清楚了。</p>

<p>key 的生成算法如下：</p>

<pre><code class="ruby">Digest::SHA1.hexdigest([session[:id], "captcha", Time.now.to_s].join)
</code></pre>

<p>value 的算法如下：</p>

<pre><code class="ruby">def generate_simple_captcha_data(code)
  value = ''
  case code
    when 'numeric' then
      SimpleCaptcha.length.times{value &lt;&lt; (48 + rand(10)).chr}
    else
      SimpleCaptcha.length.times{value &lt;&lt; (65 + rand(26)).chr}
  end
  return value
end
</code></pre>

<p>simple_captcha 实现了一个 middleware， 以返回验证码图片。</p>

<pre><code class="ruby">module SimpleCaptcha
  class Middleware
    # ...
    def call(env) # :nodoc:
      if env["REQUEST_METHOD"] == "GET" &amp;&amp; captcha_path?(env['PATH_INFO'])
        make_image(env)
      else
        @app.call(env)
      end
    end

    # ...

    def make_image(env, headers = {}, status = 404)
      request = Rack::Request.new(env)
      code = request.params["code"]
      body = []

      if !code.blank? &amp;&amp; Utils::simple_captcha_value(code)
        return send_file(generate_simple_captcha_image(code), :type =&gt; 'image/jpeg', :disposition =&gt; 'inline', :filename =&gt;  'simple_captcha.jpg')
      end

      [status, headers, body]
    end
    #...
</code></pre>

<p>在生成验证码图片这一步，它是调用了 ImageMagick 的命令来生成图片的。</p>

<pre><code class="ruby">def self.run(cmd, params = "", expected_outcodes = 0)
  command = %Q[#{cmd} #{params}].gsub(/\s+/, " ")
  command = "#{command} 2&gt;&amp;1"

  unless (image_magick_path = SimpleCaptcha.image_magick_path).blank?
    command = File.join(image_magick_path, command)
  end

  # convert -size 100x100 -gravity "Center" -implode 0.2 &lt;path/to/file&gt;
  output = `#{command}`

  unless [expected_outcodes].flatten.include?($?.exitstatus)
    raise ::StandardError, "Error while running #{cmd}: #{output}"
  end

  output
end
</code></pre>

<p><strong>总结</strong></p>

<p>分三步走：</p>

<ol>
<li>生成一组 key, value，然后存进数据库</li>
<li>浏览器根据 key 请求验证码图片</li>
<li>根据 key 从数据库里取出 value，然后判定用户提交的验证码是否正确</li>
</ol>


<h2>我是这么做的</h2>

<p>simple_captcha2 为图片验证码提供了一整套的解决方案，在 Rails 里使用非常简单。
但仍有做得不足的地方，例如没有暴露生成验证码图片的接口给我们，使得我们能够直接通过 ajax 来请求验证码图片。
虽然可以通过 mokey patch 来做的，但是我自己想了一个更简单的方案。</p>

<p>我的思路：</p>

<ol>
<li>生成验证码字符串</li>
<li>根据字符串生成验证码图片</li>
<li>将验证码字符串放到 session 里</li>
<li>将图片编码成 Base64 字符串，剩下的就看你的使用场景</li>
</ol>


<p>伪代码</p>

<pre><code class="ruby"># in someone controller

# GET /captcha
def captcha
  text = SecureRandom.hex(2).upcase
  session[:captcha] = text
  base64Image = Captcha.generate text, 126, 40  # 验证码的内容，图片的宽度，图片的高度
  render json: { image: base64Image }
end
</code></pre>

<p>最关键的就是如何实现 <code>Captcha.generate</code> 了，使用纯 Ruby 生成图片成本高，需要许多知识，所以我还是选择了依赖于 ImageMagick。
思路也很简单：</p>

<ol>
<li>根据参数，生成关于 ImageMagick 中的 <code>convert</code> 命令所需要的参数</li>
<li>使用系统调用来生成图片，并放在一个临时目录里</li>
<li>读取图片的内容，完成后关闭这个临时文件，最后把图片内容返回</li>
</ol>


<pre><code class="ruby">module Captcha
  extend self

  def generate text, width = 100, height = 28
    text = text.upcase

    params = ['-fill darkblue', '-background white']
    params &lt;&lt; "-size #{width}x#{height}"
    params &lt;&lt; "-wave #{distortion}"
    params &lt;&lt; '-gravity "Center"'
    params &lt;&lt; '-pointsize 22'
    params &lt;&lt; '-implode 0.2'

    dst = Tempfile.new(['neolion_captcha', '.png'], Dir::tmpdir)
    dst.binmode

    params &lt;&lt; "label:'#{text}' \"#{File.expand_path(dst.path)}\""

    run(params.join(' '))

    dst.close

    read_as_base64 File.expand_path(dst.path)
  end

  private

    def distortion
      [0 + rand(2), 80 + rand(20)].join('x')
    end

    def run params = "", expected_outcodes = 0
      command = %Q[convert #{params}].gsub(/\s+/, " ")
      command = "#{command} 2&gt;&amp;1"

      output = `#{command}`

      unless [expected_outcodes].flatten.include?($?.exitstatus)
        raise ::StandardError, "Error while running #{command}"
      end

      output
    end

    def read_as_base64 filepath
      data = Base64.encode64(File.binread(filepath))
      ['data:image/png;base64,', data].join.gsub(/\n/, '')
    end
end
</code></pre>

<p>至于判定用户的验证码是否正确，就很简单了</p>

<pre><code class="ruby">class ApplicationController &lt; ActionController::Base
  # ...

  def valid_captcha?
    session[:captcha] == params[:captcha]
  end

  # ...
end
</code></pre>

<h2>References</h2>

<ul>
<li><a href="http://en.wikipedia.org/wiki/Digital_image">http://en.wikipedia.org/wiki/Digital_image</a></li>
<li><a href="http://www.webascender.com/Blog/ID/529/Working-with-Bits-and-Bytes-in-Ruby#.VUuBvROUe58">http://www.webascender.com/Blog/ID/529/Working-with-Bits-and-Bytes-in-Ruby#.VUuBvROUe58</a></li>
<li><a href="http://www.imagemagick.org/script/index.php">http://www.imagemagick.org/script/index.php</a></li>
<li><a href="https://github.com/minimagick/minimagick">https://github.com/minimagick/minimagick</a></li>
<li><a href="https://github.com/minimagick/minimagick/issues/59">https://github.com/minimagick/minimagick/issues/59</a></li>
<li><a href="https://github.com/wvanbergen/chunky_png">https://github.com/wvanbergen/chunky_png</a></li>
<li><a href="https://github.com/wvanbergen/oily_png">https://github.com/wvanbergen/oily_png</a></li>
</ul>

]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[Threads in Ruby（译）]]></title>
    <link href="http://www.justqyx.me/blog/2014/12/31/threads-in-ruby/"/>
    <updated>2014-12-31T14:27:04+08:00</updated>
    <id>http://www.justqyx.me/blog/2014/12/31/threads-in-ruby</id>
    <content type="html"><![CDATA[<p><a href="http://www.sitepoint.com/threads-ruby/">这是原文</a></p>

<p>Ruby 有着许多吸引开发者的 cool features，例如在运行时能够创建类、修改特定对象的行为、使用 ObjectSpace 监控内存中类的数量以及大量的测试套件。这些东西使得开发者的生活更加轻松。今天我们将会讨论在计算机科学里最重要的概念之一：线程，以及 Ruby 是如何支持线程的。</p>

<h2>简介</h2>

<p>首先，让我们来定义“线程”（thread），请参考 <a href="http://en.wikipedia.org/wiki/Thread_(computing)">wikipedia</a></p>

<blockquote><p>In computer science, a thread of execution is the smallest sequence of programmed instructions that can be managed independently by an operating system scheduler. A thread is a light-weight process.</p></blockquote>

<p>线程可以堪称是一个轻量级（light-weight）的进程。
多个线程属于同个进程，并且共享着进程的资源（resources）。
这就是为什么在有些情况下使用多线程是比较经济（economical）的。</p>

<p>现在让我们来看看线程对于我们来说是多么有用。</p>

<h2>Basic Example</h2>

<p>看看下面的代码</p>

<pre><code class="ruby">def calculate_sum(arr)
  sum = 0
  arr.each do |item|
    sum += item
  end
  sum
end

@items1 = [12, 34, 55]
@items2 = [45, 90, 2]
@items3 = [99, 22, 31]

puts "items1 = #{calculate_sum(@items1)}"
puts "items2 = #{calculate_sum(@items2)}"
puts "items3 = #{calculate_sum(@items3)}"
</code></pre>

<p>上面的代码输出的结果为</p>

<pre><code class="ruby">items1 = 101
items2 = 137
items3 = 152
</code></pre>

<p>这是一个非常简单的程序，有助于弄明白我们为什么要使用多线程。在上面的代码里，我们有三个数组，并且要计算出它们的和，但有一个问题，我们无法在 items1 算出结果之前，得到 items2 的结果，
对于 items3 也同样存在这个问题。现在，让我们修改一下代码来进一步说明我想表达的东西。</p>

<pre><code class="ruby">def calculate_sum(arr)
  sleep(2)
  sum = 0
  arr.each do |item|
    sum += item
  end
  sum
end
</code></pre>

<p>在上面的代码中，我们增加了一个 <code>sleep(2)</code> 指令，让程序暂停2秒钟之后再继续运行。
这意味着，items1 会在2秒后才得到结果，items2 会在4秒后得到结果，而 items3 将会在6秒后得到结果。
这并不是我们想要的。</p>

<p>上面的三个数组，对与彼此都没有依赖，所以它们是能够独立地进行元素和的计算，这时候多线程就可以派上用场了。</p>

<p>多线程使得我们可以将当前的程序分割成为几个不同的、能够独立运行的上下文执行体（execution contexts）。
现在，让我们来将上面的程序修改成为一个多线程的版本。</p>

<pre><code class="ruby">def calculate_sum(arr)
  sleep(2)
  sum = 0
  arr.each do |item|
    sum += item
  end
  sum
end

@items1 = [12, 34, 55]
@items2 = [45, 90, 2]
@items3 = [99, 22, 31]

threads = (1..3).map do |i|
  Thread.new(i) do |i|
    items = instance_variable_get("@items#{i}")
    puts "items#{i} = #{calculate_sum(items)}"
  end
end
threads.each {|t| t.join}
</code></pre>

<p>我们把每一个数组元素和的计算工作，都交给一个线程来做，通过 <code>Thread.new</code> 的方式来创建线程，
并且把数组元素和的计算的代码段放到代码块中里去，这样我们一共新建了3个线程。
现在让我们来看看运行的结果，注意每次的运行结果顺序可能有所不同。</p>

<pre><code class="ruby">items2 = 137
items3 = 152
items1 = 101
</code></pre>

<p>从运行结果来看，我们不是在4秒后收到items2的结果，6秒钟之后收到items3的结果，而是在2秒钟之后收到所有数组的结果。这让我们看到了多线程的魔力，我们是在并行地计算所有数组的元素和，而不是一次只计算一个。这是非常棒的，因为我们节省了4秒钟的时间，提高了程序的性能和效率。</p>

<h2>竞态（Race Conditions）</h2>

<p>每一个功能其实都是有代价的，多线程是好东西，但是如果你在写多线程的代码的时候，你需要了解<strong>竞态条件</strong>这个东西，那么什么是竞态呢？请参考 <a href="http://en.wikipedia.org/wiki/Race_condition#Software">wikipedia</a></p>

<blockquote><p>Race conditions arise in software when separate computer processes or threads of execution depend on some shared state. Operations upon shared states are critical sections that must be mutually exclusive. Failure to obey this rule opens up the possibility of corrupting the shared state.</p></blockquote>

<p>简单地来讲，如果我们有一些共享的数据，这些数据能够被多线程使用的话，那么我们的数据在所有的线程执行完毕之后还应该保持正确不出错。</p>

<p><strong>例子</strong></p>

<pre><code class="ruby">class Item
  class &lt;&lt; self; attr_accessor :price end
  @price = 0
end

(1..10).each { Item.price += 10 }

puts "Item.price = #{Item.price}"
</code></pre>

<p>我们创建了一个简单的 Item 类，包含了一个类变量 price。<code>Item.price</code> 的值在一个循环力不断地增加。
执行这个程序，然后你将会看到一下的输出</p>

<pre><code class="ruby">Item.price = 100
</code></pre>

<p>现在，让我们来看看一个多线程的版本</p>

<pre><code class="ruby">class Item
  class &lt;&lt; self; attr_accessor :price end
  @price = 0
end

threads = (1..10).map do |i|
  Thread.new(i) do |i|
    item_price = Item.price # Reading value
    sleep(rand(0..2))
    item_price += 10        # Updating value
    sleep(rand(0..2))
    Item.price = item_price # Writing value
  end
end

threads.each {|t| t.join}

puts "Item.price = #{Item.price}"
</code></pre>

<p>我们的 Item 类还是一样的，但是，我们已经改变了 price 的递增方式。我们在代码里使用了 sleep 来向你展示在并发中可能出现的问题。多跑几次程序，你将会得到不同的结果。</p>

<pre><code class="ruby">Item.price = 40
</code></pre>

<p>多次运行我们的程序下来，我们会发现，结果不再正确，并且结果是变化的。
输出的不再是 100，你看到的可能是30、40或者70等。
这就是竞态所导致的结果。</p>

<h2>互斥体</h2>

<p>为了解决竞态所产生的问题，我们必须要控制程序，
当一个线程正在工作时，另外一个线程必须等待，直到当前的工作进程完成工作。
这个行为我们称为互斥，下面我们将阐述如何去使用互斥体。</p>

<p>Ruby 已经为我们使用互斥体提供了一个非常简洁的方式，如下：</p>

<pre><code class="ruby">class Item
  class &lt;&lt; self; attr_accessor :price end
  @price = 0
end

mutex = Mutex.new

threads = (1..10).map do |i|
  Thread.new(i) do |i|
    mutex.synchronize do
      item_price = Item.price # Reading value
      sleep(rand(0..2))
      item_price += 10        # Updating value
      sleep(rand(0..2))
      Item.price = item_price # Writing value
    end
  end
end

threads.each {|t| t.join}

puts "Item.price = #{Item.price}"
</code></pre>

<p>现在运行这个程序，你将会看到</p>

<pre><code class="ruby">Item.price = 100
</code></pre>

<p>这是因为 <code>mutex.synchronize</code>，在任何时候，每一个都有且仅有一个线程能够执行 <code>mutex.synchronize</code> 里的代码块，其他的线程必须等待直到当前的工作线程执行完毕。</p>

<p>这样，我们的代码就是<strong>线程安全</strong>了。</p>

<p>Rails 是线程安全的，当多个线程尝试去运行同样的代码是，它使用了 Mutex 类的一个实例来避免
竞态的出现。请查看 <code>Rack::Lock</code> 这个中间件里的代码，你会发现它使用了 <code>@mutext.lock</code> 来阻塞其他尝试想要运行相同代码的线程。如果你想要了解 Rails 里更多关于多线程的细节，
请阅读我的<a href="http://www.sitepoint.com/config-threadsafe/">这篇文章</a>。
另外，你也可以访问 <a href="http://www.ruby-doc.org/core-2.0.0/Mutex.html">Ruby Mutext</a> 这个类有关的文档。</p>

<h2>不同 Ruby 版本的线程类型</h2>

<p>在 Ruby 1.8 版本里，Ruby 的实现是<strong>绿色线程</strong>（green threads）。绿色线程是由解释器来实现和控制的，下面是一些关于绿色线程的优点和缺点：</p>

<p><strong>优点</strong></p>

<ul>
<li>Cross platform (managed by the VM)</li>
<li>Unified behavior / control</li>
<li>Lightweight -> faster, smaller memory footprint</li>
</ul>


<p><strong>缺点</strong></p>

<ul>
<li>Not optimized</li>
<li>Limited to 1 CPU</li>
<li>A blocking I/O blocks all threads</li>
</ul>


<p>到了 Ruby 1.9 版本后，Ruby 使用的是原生线程（native threads）。原生线程的意思是每个线程都是由 Ruby 创建的，并且直接映射到操作系统级别的线程。每一门现代编程语言都实现了 native threads，所以使用 native threads 是更加直接和合理的。下面是一些关于 native threads 的优点：</p>

<p><strong>优点</strong></p>

<ul>
<li>Run on multiple processors</li>
<li>Scheduled by the OS</li>
<li>Blocking I/O operations don’t block other threads</li>
</ul>


<p>虽然在 Ruby 1.9 里使用的是 native threads，但是即使我们的处理器是多核的，
每次运行时都仍然只有一个线程能运行。这是因为 MRI 的 GIL (Global Interpreter Lock)
或者说 GVL (Global VM Lock)。在 Ruby 中，如果有一个线程正在运行，
那么 GIL 就会阻止其他线程运行。但是 Ruby 足够聪明来切换到其他正在等待的线程，
如果当前正在运行的线程正在等待一些 I/O 操作完成的话。</p>

<p>在 Ruby 中使用线程是非常容易的，但是我们必须谨慎地处理好并发可能产生地问题。
我希望你能喜欢这篇文章并且在 Ruby 中能够更好地使用好线程。</p>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[Stub and Mock in Minitest]]></title>
    <link href="http://www.justqyx.me/blog/2014/11/06/minitest/"/>
    <updated>2014-11-06T20:32:48+08:00</updated>
    <id>http://www.justqyx.me/blog/2014/11/06/minitest</id>
    <content type="html"><![CDATA[<p>Stubs 和 Mocks 在写测试时，经常会需要用到，
而 minitest（5.4.x） 文档在这两个地方有点难懂。</p>

<p>官方的对 Stub 的实现很弱，基本不可用，
有个 <em>minitest-stub_any_instance</em> 扩展可以满足我们的需要，</p>

<pre><code>gem install minitest-stub_any_instance -v '1.0.0'
</code></pre>

<p>来看例子：</p>

<pre><code class="ruby">require "minitest/autorun"
require "minitest/stub_any_instance"

class Book
  def initialize name; @name = name; end;
  def name; @name; end
end

describe Book do
  it "stub example" do
    Book.stub_any_instance :name, "Computer Systems" do
      book = Book.new "SICP"
      book.name.must_equal "Computer Systems"
    end
  end
end
</code></pre>

<p>而对于 mock，官方的实现不错，下面是一个例子：</p>

<pre><code class="ruby">require "minitest/autorun"

class Book
  def initialize name; @name = name; end;
  def name; @name; end
end

describe Book do
  it "mock example" do
    book = MiniTest::Mock.new
    book.expect :name, "Computer Systems"
    book.expect :price, 100
    book.expect :can_buy?, false, [80]

    book.name.must_equal "Computer Systems"
    book.price.must_equal 100
    book.can_buy?(80)
    book.verify
  end
end
</code></pre>

<p><a href="http://docs.seattlerb.org/minitest/Minitest/Mock.html#method-i-expect">官方的文档</a>里有 <code>expect</code> 和 <code>verify</code> 这两个函数的文档。</p>
]]></content>
  </entry>
  
</feed>
